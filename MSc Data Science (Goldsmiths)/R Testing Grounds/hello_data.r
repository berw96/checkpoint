# Task 1: create a data frame with the following fields
# ID, Username, Games Owned, Registration Date
my_data <- data.frame(
  ID = c(1:5),
  Username = c("xxellot","Mr_Lootbox","DawnsAtNight","nKm90","Jams"),
  Games_Owned = c(10, 11, 2, 9, 0),
  Registration_Date = c(1:5)
)

print(my_data)


# Task 2: query only the Username and Library columns.




# Task 3: add a new row to the data frame and write to a .csv file.
